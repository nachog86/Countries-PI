import React from 'react';
import styles from './Spinner.module.css';  // Asegúrate de crear este archivo

const Spinner = () => {
  return (
    <div className={styles.spinner}></div>
  );
};

export default Spinner;

